// .sort(compareFunction): Sorts an array (modifies original array).
// .reverse(): Reverses the array in place.

let arrr = [1,4,5,3,9,8,4,5];

console.log(arrr.sort());
console.log(arrr.reverse());
