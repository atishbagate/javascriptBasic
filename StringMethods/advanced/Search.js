console.log("Search");

let str = "This is the string to search by using regEx.";

// syntax 
// .search(RegExp);
// return the index of first ocuurence 

console.log(str.search("the"));
console.log(str.search(/ring/));
console.log(str.search(/z/));
